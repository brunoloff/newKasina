#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
project_dir=$(dirname -- "$script_dir")
export CARGO_HOME="$project_dir/.cargo-local"
if [ -d "$project_dir/.rustup-local/toolchains" ]; then
    export RUSTUP_HOME="$project_dir/.rustup-local"
fi

exec cargo "$@"
